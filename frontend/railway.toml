[build]
  builder = "NIXPACKS"
  buildCommand = "npm run build"

[deploy]
  startCommand = "npx serve -s dist -l $PORT"
  healthcheckPath = "/"
  healthcheckTimeout = 100

[[services]]
  name = "frontend"

[env]
  PORT = "3000"
